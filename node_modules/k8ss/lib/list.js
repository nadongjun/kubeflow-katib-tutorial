"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getSubDirs = exports.list = void 0;
const fs_1 = require("fs");
const os_1 = __importDefault(require("os"));
const path_1 = __importDefault(require("path"));
exports.list = () => {
    try {
        return (exports.getSubDirs(path_1.default.join(os_1.default.homedir(), 'k8s-config')).filter(x => x !== '.git').join('\n'));
    }
    catch (ex) {
        return 'No configured clusters found. You can create an folder named `k8s-config` under your home directory, which contains the cluster config files inside each subfolder whose name is the cluster name.';
    }
};
exports.getSubDirs = (parentDir) => fs_1.readdirSync(parentDir, { withFileTypes: true }).filter(x => x.isDirectory()).map(x => x.name);
